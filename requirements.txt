# Core dependencies
asyncio-mqtt>=0.13.0
bleak>=0.20.0
cryptography>=41.0.0
websockets>=11.0.0
aiohttp>=3.8.0
aiofiles>=23.0.0

# Database and storage
aiosqlite>=0.19.0
sqlalchemy[asyncio]>=2.0.0

# Compression
lz4>=4.0.0

# Geohash for location-based channels
pygeohash>=1.3.0

# CLI interface
curses>=2.2  # Built-in with Python, but listed for clarity
readline>=8.0  # Built-in with Python, but listed for clarity

# Configuration
configparser>=6.0.0  # Built-in with Python, but listed for clarity

# Logging and monitoring
structlog>=23.0.0

# Development and testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
pre-commit>=3.3.0

# Optional dependencies for enhanced functionality
# Uncomment if needed:
# pyserial>=3.5  # For GPIO-based emergency wipe
# psutil>=5.9.0  # For system monitoring
# python-daemon>=3.0.0  # For background service management